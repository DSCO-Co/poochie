import type { IronSessionOptions } from 'iron-session';
export declare const sessionOptions: IronSessionOptions;
declare module "iron-session" {
    interface IronSessionData {
        customerId: number;
        token: string;
    }
}
